# Week 3 · Now You Try solutions

This is the finished lecture code plus answers to both Now You Tries. Try
them yourself before you look.

| Now You Try | Where |
|---|---|
| **#1** — `findPricierTest`, `updatePriceTest`, `deleteByNameTest`, and the stretch `markOnSaleTest` | the bottom of `src/db-explore.ts`, under "Now You Try #1" |
| **#2** — tests for PATCH and DELETE | `src/__tests__/products-changes.test.ts` |
| **#2 stretch** — reject malformed ids with a 400 | `src/validate-id.ts`, used by the three `/:id` routes in `src/product-router.ts`; tested at the end of `products-changes.test.ts` |

Run the tests with `npm test`. Run the Now You Try #1 functions with
`npm run explore`, after pointing `main()` at the one you want.
